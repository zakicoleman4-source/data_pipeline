name: Build Windows .exe

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write  # required by softprops/action-gh-release to create / attach releases

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: pyinstaller --noconfirm data_to_frames.spec

      - name: Verify build output
        run: |
          if (-not (Test-Path "dist/client_pipeline/client_pipeline.exe")) {
            Write-Error "client_pipeline.exe missing"
            exit 1
          }
          if (-not (Test-Path "dist/client_pipeline/_internal/vendor/ffmpeg/bin/ffmpeg.exe")) {
            Write-Error "bundled ffmpeg missing"
            exit 1
          }

      - name: Zip dist folder
        run: Compress-Archive -Path dist/client_pipeline/* -DestinationPath client_pipeline-windows.zip

      - name: Upload build artifact
        uses: actions/upload-artifact@v5
        with:
          name: client_pipeline-windows
          path: client_pipeline-windows.zip
          retention-days: 30

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: client_pipeline-windows.zip
